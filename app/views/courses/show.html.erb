<h4>Course</h4>
<br/>
<div>Subject: <%= @course.subject%></div>
<div>Lecturer: <%= @lecturer.fullname%></div>
<div>Students Enrolled: <%= @course.student.count %></div>

Roll Call:

<%= @course.students.each do |student|%>
    <div><%= link_to student.fullname, student_path(student) %></div>
<%end%>